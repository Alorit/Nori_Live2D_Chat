# 核心依赖（Python 3.10+，推荐 3.11/3.12/3.13）
PySide6>=6.10
openai>=1.50
numpy>=1.26
jieba>=0.42
pyyaml>=6.0
requests>=2.31
sounddevice>=0.4.6

# MCP 技能客户端（Skills 用）
mcp>=2.0

# 可选：向量长期记忆（需要 PyTorch，体积较大，默认用 BM25 即可）
# sentence-transformers>=3.0

# 说明：TTS 只保留 GPT-SoVITS（本地 API，独立安装）+ Windows SAPI5（系统自带，无需依赖）；
# 旧的 sherpa-onnx / piper-tts / edge-tts 后端已移除，备份见 D:/Nori_Backup/
